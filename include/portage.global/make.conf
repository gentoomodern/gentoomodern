ZENTOO_COMMON_FLAGS="-pipe -O2 ${ZENTOO_ARCH_FLAGS}"
COMMON_FLAGS="-pipe -O2 ${ZENTOO_COMMON_FLAGS}"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

EMERGE_DEFAULT_OPTS="--buildpkg --usepkg --quiet --verbose"
PORTAGE_RSYNC_EXTRA_OPTS="--exclude ChangeLog* --delete-excluded"

FEATURES="binpkg-multi-instance buildpkg userpriv usersync xattr parallel-fetch -pid-sandbox -ipc-sandbox -network-sandbox"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

