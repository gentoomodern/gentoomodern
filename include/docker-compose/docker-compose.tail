networks:
#  frontend:
#    driver: overlay
  backend:
    driver: overlay

volumes:
  distfiles:
    driver: local
  ebuilds:
    driver: local
  binpkgs:
    driver: local
  kernels:
    driver: local
#  stage-packing:
#    driver: local
