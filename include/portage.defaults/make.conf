COMMON_FLAGS="${ZENTOO_COMMON_FLAGS}"
CFLAGS="${COMMON_FLAGS} ${ZENTOO_CFLAGS}"
CXXFLAGS="${COMMON_FLAGS} ${ZENTOO_CXXFLAGS}"
FCFLAGS="${COMMON_FLAGS} ${ZENTOO_FCFLAGS}"
FFLAGS="${COMMON_FLAGS} ${ZENTOO_FFLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

FEATURES="binpkg-multi-instance buildpkg userpriv usersync xattr parallel-fetch -pid-sandbox -ipc-sandbox -network-sandbox"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C
